pc.extend(pc, (() => {
    /**
     * @private
     * @constructor
     * @name pc.ComponentData
     * @classdesc Base class for Component data storage.
     */
    const ComponentData = () => {
    };

    return {
        ComponentData
    };
})());
